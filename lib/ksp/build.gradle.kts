// This is a placeholder for root project plugins, if you use version catalogs,
// you would configure kotlin and ksp plugins here.
// For this demo, versions are managed in subprojects for simplicity.
plugins {
    // We apply the plugins in the subprojects directly
}
