plugins {
    id("site.addzero.buildlogic.kmp.platform.kmp-core") version "+"
//    id("kmp-koin-core")

}

kotlin {
    sourceSets {
        commonMain.dependencies {
            // 基础依赖
        }
    }
}
