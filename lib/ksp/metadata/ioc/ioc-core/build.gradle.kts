plugins {
    id("site.addzero.gradle.plugin.kmp-core-convention") version "+"
//    id("kmp-koin-core")

}

kotlin {
    sourceSets {
        commonMain.dependencies {
            // 基础依赖
        }
    }
}
