package site.addzero.gradle.plugin

import org.gradle.api.Plugin
import org.gradle.api.Project

class IntellijBasePlugin : Plugin<Project> {
    override fun apply(project: Project) {
        // Empty plugin implementation
    }
}