# kcloud-paths

JVM path-related support module reserved for KCloud runtime path logic.

- Module path: `/Users/zjarlin/IdeaProjects/addzero-lib-jvm/lib/kcloud-paths`
- Publish target: Maven Central via `addzero-lib-jvm`

Minimal usage:

- This module is currently a thin placeholder module with no exported API surface yet.

Runtime constraints:

- JVM-only module
- Primarily used as a dependency boundary for `kcloud-core`
