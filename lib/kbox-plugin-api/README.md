# kbox-plugin-api

Public plugin API for KBox extensions, runtime contracts, and route contribution models.

- Module path: `/Users/zjarlin/IdeaProjects/addzero-lib-jvm/lib/kbox-plugin-api`
- Publish target: Maven Central via `addzero-lib-jvm`

Minimal usage:

```kotlin
import site.addzero.kbox.plugin.api.KboxPluginContext
import site.addzero.kbox.plugin.api.KboxRuntimePlugin
```

Runtime constraints:

- Kotlin Multiplatform module
- Intended for plugin authors and runtime consumers
