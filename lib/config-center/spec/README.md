# config-center-spec

Contracts and shared models for the Config Center library family.

- Module path: `/Users/zjarlin/IdeaProjects/addzero-lib-jvm/lib/config-center/spec`
- Publish target: Maven Central via `addzero-lib-jvm`

Minimal usage:

```kotlin
import site.addzero.configcenter.spec.ConfigCenterGateway
import site.addzero.configcenter.spec.ConfigQuery
```

Runtime constraints:

- Kotlin Multiplatform module
- Base contract module for `config-center-client`, `config-center-runtime-jvm`, and `config-center-ktor`
