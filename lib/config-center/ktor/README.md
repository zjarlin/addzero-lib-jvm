# config-center-ktor

Ktor integration layer for exposing Config Center routes and handlers.

- Module path: `/Users/zjarlin/IdeaProjects/addzero-lib-jvm/lib/config-center/ktor`
- Publish target: Maven Central via `addzero-lib-jvm`

Minimal usage:

```kotlin
import site.addzero.configcenter.ktor.configCenterRoutes
```

Runtime constraints:

- JVM/Ktor server module
- Depends on `config-center-spec` and `config-center-runtime-jvm`
