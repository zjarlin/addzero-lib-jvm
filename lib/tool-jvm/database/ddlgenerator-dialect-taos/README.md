# ddlgenerator-dialect-taos

- 作用：提供 TAOS / TDengine 的 AutoDDL 方言实现。
- Maven 坐标：`site.addzero:ddlgenerator-dialect-taos`
- 本地路径：`lib/tool-jvm/database/ddlgenerator-dialect-taos`
- 约束：当前仅覆盖普通表 + 列 + comment 的关系型子集，不建模 super table/tag。
