package site.addzero.ide.ui.form

import javax.swing.JPanel

/**
 * 动态表单构建器
 * 用于构建配置界面的表单面板
 */
class DynamicFormBuilder {
    // 这里应该是实际的表单构建逻辑
    // 由于我们只是修复导入错误，所以提供一个简单的实现
    
    fun build(): JPanel {
        return JPanel()
    }
}